<UserControl x:Class="LiteDB.Studio.Wpf.Controls.ResultGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Warning: Results limited to 1000 rows" Visibility="{Binding QueryResult.LimitExceeded, Converter={StaticResource BoolToVis}}" Background="Yellow" Padding="5" />
        <DataGrid x:Name="ResultsDataGrid"
                  Grid.Row="1"
                  ItemsSource="{Binding QueryResult.Rows}"
                  VirtualizingStackPanel.IsVirtualizing="True"
                  AutoGenerateColumns="False"
                  IsReadOnly="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow">
        </DataGrid>
    </Grid>
</UserControl>