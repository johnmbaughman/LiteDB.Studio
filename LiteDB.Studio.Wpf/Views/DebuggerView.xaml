<UserControl
    x:Class="LiteDB.Studio.Wpf.Views.DebuggerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="120"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <StackPanel Margin="6" Orientation="Vertical">

        <!--  Header  -->
        <TextBlock
            Margin="0,0,0,6"
            FontSize="12"
            FontWeight="SemiBold"
            Text="Database Debugger" />

        <!--  Status row  -->
        <StackPanel Orientation="Horizontal">
            <Ellipse
                Width="10"
                Height="10"
                Margin="0,0,6,0"
                VerticalAlignment="Center">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="#cccccc" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                <Setter Property="Fill" Value="#4CAF50" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
            <TextBlock VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Text" Value="Stopped" />
                        <Setter Property="Foreground" Value="Gray" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                <Setter Property="Text" Value="{Binding Port, StringFormat='Running on port {0}'}" />
                                <Setter Property="Foreground" Value="Black" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>

        <!--  Action buttons  -->
        <StackPanel Margin="0,8,0,0" Orientation="Horizontal">
            <Button
                MinWidth="60"
                Margin="0,0,4,0"
                Command="{Binding StartCommand}"
                Content="Start"
                ToolTip="Start the page-dump HTTP server" />
            <Button
                MinWidth="60"
                Margin="0,0,4,0"
                Command="{Binding StopCommand}"
                Content="Stop"
                ToolTip="Stop the page-dump HTTP server" />
            <Button
                MinWidth="80"
                Command="{Binding OpenBrowserCommand}"
                Content="Open Browser"
                ToolTip="Open the debugger in the system browser" />
        </StackPanel>

    </StackPanel>
</UserControl>
